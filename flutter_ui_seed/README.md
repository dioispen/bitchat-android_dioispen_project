# Flutter UI Seed

這個資料夾是「Flutter module 建立後要覆蓋進 `flutter_ui/lib/` 的 UI 範本」。

- `scripts/flutter/create_module.ps1` 會在 `flutter create -t module flutter_ui` 完成後，把本資料夾的 `lib/` 複製到 `flutter_ui/lib/`。
- 目的：讓你一生成 module 就有可跑的聊天室 Demo UI + Channel 骨架。

